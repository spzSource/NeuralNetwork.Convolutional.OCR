<UserControl x:Class="DigitR.Pages.Teach.Steps.StartTeachingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding StartTeachingViewModel, Source={StaticResource Locator}}">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel 
                MinWidth="200" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
                
                <Image Margin="10" Height="200" Width="200" Source="{Binding ProcessedImage}"/>
                
                <Button 
                    Command="{Binding Path=ProcessTrainingCommand}"
                    Content="Start training"
                    Width="100"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
